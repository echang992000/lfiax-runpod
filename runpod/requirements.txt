# Pin the JAX/lfiax stack used by sir.py / cli-anything-lfiax on a RunPod
# CUDA pod. JAX 0.7.x is the version that lfiax + the harness were
# developed against locally; pair it with the matching jax-cuda12 plugin.
# Distrax/dm-haiku/optax pinned to versions that import cleanly under JAX
# 0.7.x (verified locally — same versions as the boed_agent_3 venv).

# JAX with CUDA 12 GPU plugin. RunPod CUDA pods ship CUDA 12.x runtime;
# `jax[cuda12]` pulls in matching jaxlib + the cuda plugin from PyPI.
jax[cuda12]==0.7.1
jaxlib==0.7.1
ml_dtypes>=0.5.0

# Sampling / flow stack used by sir.py.
blackjax==1.3
distrax==0.1.8
dm-haiku==0.0.16
optax==0.2.8
chex>=0.1.91
tensorflow_probability
tensorflow
tensorflow_datasets

# torch must match a CUDA build available on RunPod images; the +cu121
# wheel is what the standard PyTorch CUDA 12.1 RunPod template ships
# with. Override at install time if your pod uses a different CUDA.
torch==2.1.2
torchsde==0.2.6

# Hydra + wandb + simulation-based inference tools that sir.py imports.
hydra-core==1.3.2
hydra-submitit-launcher==1.2.0
omegaconf==2.3.0
wandb
sbi==0.26.1

# General scientific stack pinned to versions our local venv used.
numpy<2
matplotlib
tqdm
